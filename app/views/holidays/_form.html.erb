<%= form_with(model: holiday, local: true) do |form| %>
  <% if holiday.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(holiday.errors.count, "error") %> prohibited this holiday from being saved:</h2>

      <ul>
        <% holiday.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :date_as %>
    <%= form.date_select :date_as %>
  </div>

  <div class="field">
    <%= form.label :regional %>
    <%= form.number_field :regional %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
